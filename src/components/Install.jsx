import React from 'react';

function Install() {
  const styles = {
    wrapperStyle: {
      width: '70vw'
    }
  }
  return(
    <div style={styles.wrapperStyle}>
      <h1>Install</h1>
    </div>
  );
}

export default Install;
